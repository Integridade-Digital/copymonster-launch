{
  "name": "@copymonster/tenant-context",
  "version": "1.0.0",
  "description": "Tenant context manager for CopyMonster SaaS - Sets DB context for RLS policies",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "test": "jest"
  },
  "dependencies": {
    "@copymonster/supabase-client": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
