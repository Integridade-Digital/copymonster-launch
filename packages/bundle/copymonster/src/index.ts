/**
 * @deepseek-ai/dsh-bundle-copymonster — the CopyMonster SaaS layer.
 *
 * This package carries no runtime API: its substance is `cordis.patch.yml`,
 * which inserts `@deepseek-ai/dsh-api-auth-context` and
 * `@deepseek-ai/dsh-api-auth-http` into the composing profile. Each inserted
 * row's own package owns that row's behavior.
 * @module @deepseek-ai/dsh-bundle-copymonster
 */

export {}
